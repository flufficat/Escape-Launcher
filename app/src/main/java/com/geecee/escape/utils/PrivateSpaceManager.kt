package com.geecee.escape.utils

import android.content.Context
import android.content.pm.LauncherApps
import androidx.core.content.ContextCompat.getSystemService

class PrivateSpaceManager {
    fun isPrivateSpace(){
//        val launcherApps = getSystemService(Context.LAUNCHER_APPS_SERVICE) as LauncherApps
//
//        val launcherUserInfo = launcherApps.getLauncherUserInfo()
//        if (launcherUserInfo.isQuietModeEnabled) {
//            // Handle private space locked state
//        } else {
//            // Handle private space unlocked state
//        }
    }
}